plugins {
    id "java"
}

project.ext {
    app_name = "roman_numerals"
    build_jar = project.name + ".jar"
    app_jar = app_name + ".jar"
}

def main_class = "RomanNumerals"

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes 'Main-Class': main_class
    }
}
