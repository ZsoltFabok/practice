project.ext {
    app_name = "text_munger"
    build_jar = project.name + ".jar"
    app_jar = app_name + ".jar"
}

def main_class = "TextMunger"

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes 'Main-Class': main_class
    }
}
